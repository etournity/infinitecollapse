{
  "name": "gamejam",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel serve index.html",
    "build:parcel": "parcel build index.html",
    "compress": "bread-compressor dist",
    "build": "yarn run build:parcel && yarn run compress"
  },
  "dependencies": {
    "bread-compressor-cli": "^1.1.0",
    "parcel-bundler": "1.12.4"
  },
  "devDependencies": {
    "sass": "^1.26.11"
  }
}
